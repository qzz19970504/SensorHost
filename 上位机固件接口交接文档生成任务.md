# Goal：生成上位机固件接口交接手册

请把以下工作创建为一个 Goal 并持续执行，直到文档生成、静态核验完成且满足全部验收标准。

## 角色与目标

你是一名熟悉嵌入式通信协议、串口/TCP 流处理和桌面上位机开发的资深工程师。请基于三个现有仓库进行静态取证，为后续上位机开发人员编写一份简短、准确、可直接用于开发和联调的中文交接手册。

文档重点回答：固件怎样连接、怎样启动和控制、会发送什么数据、上位机应如何解析、异常时怎样诊断和恢复。不要评价或改造现有上位机 UI。

## 仓库与职责

1. `D:\Codes\STM32\STM32407ZG_JY61PL_II3DWB_UART_V2`
   - STM32F407 震动与姿态采集固件。
   - 这是设备状态、SDF1 协议、AT 命令、实时流、SD 存档和导出行为的主要事实源。
2. `D:\Codes\ESP32\ESP32_WIFI_UART_BRIDGE`
   - ESP32 UART 与 Wi-Fi/TCP 透明传输固件。
   - 这是 Wi-Fi 配置、TCP 连接、UDP 唤醒/发现和双向透明传输行为的主要事实源。
3. `D:\Codes\SensorHost`
   - 当前半成品上位机。
   - 仅将其协议解析器、连接层、控制流程和测试作为参考实现；其 UI 和现有行为不是协议权威。

开始时记录三个仓库当前的分支和 HEAD 提交号，并在最终手册的“文档基线”中列出，以便交接后追溯。

## 事实判定规则

按以下优先级判断事实：

1. 当前实现代码及直接验证该行为的自动化测试。
2. 黄金帧、协议参考实现和验收脚本。
3. 当前 README、协议说明和其他现行文档。
4. 半成品上位机的实际行为。
5. 历史计划、旧文档和设计草案只作线索，不作为已实现事实。

如果不同来源存在冲突，必须回到实现和测试交叉确认，并在手册中采用已验证的当前行为。静态分析仍无法确认时，明确写为“待确认”；必须依赖设备才能验证时，写为“待实机确认”。不得凭经验补全协议或假定未实现能力。

## 执行限制

- 只做静态分析：不得连接、烧录、配置或控制任何硬件。
- 不得修改 STM32、ESP32 或上位机的实现代码、测试和现有文档。
- 只允许新增或更新这一份最终文件：
  `D:\Codes\SensorHost\docs\上位机固件接口交接手册.md`
- 不创建额外附录、示例工程、脚本、图片或中间报告。
- 不搜索互联网；全部事实必须来自上述三个本地仓库。
- 不提交 Git commit，除非用户另行明确要求。

## 优先检查的资料

先用 `rg --files`、`rg` 和 Git 检查仓库结构、现状与引用关系，再重点核对以下资料及其引用到的实现文件：

- STM32：`README.md`、`docs/PROTOCOL.md`、`app/Protocol/`、`app/Control/`、`app/Transport/`、`app/Storage/`、`test/`、`test/host/`、`test/golden/`。
- ESP32：`README.md`、`main/`、`main/Kconfig.projbuild`、`components/uart_pt/`、`components/wifi_module/`、`sdkconfig.defaults`。
- 上位机：`README.md`、`docs/PROTOCOL.md`、`docs/ESP32_GATEWAY_REQUIREMENTS.md`、`src/sensor_host/protocol/`、`src/sensor_host/transport/`、`src/sensor_host/acquisition/`、相关测试和黄金帧。

不要只汇总 README；关键字节布局、命令语义、默认值、超时/重试和错误处理必须至少由代码或测试再确认一次。

## 最终文档要求

文档标题为“上位机固件接口交接手册”，目标读者是接手开发新上位机的工程师。以最短篇幅覆盖完成接入所必需的信息，避免大段复制源码、重复表述和无关开发历史。

按以下顺序组织内容；可合并过短小节，但不得遗漏实质内容：

### 1. 文档基线与适用范围

- 三个仓库的路径、职责、分支和 HEAD。
- 本文只基于静态分析，列出所有“待确认”与“待实机确认”项。
- 说明现有上位机只是参考实现。

### 2. 系统链路与职责边界

- 用一段简洁的 Mermaid 流程图表示：传感器 → STM32 → USB CDC，或 STM32 UART → ESP32 → Wi-Fi/TCP → 上位机；控制命令沿相反方向返回。
- 解释 STM32、ESP32、上位机分别负责什么，以及 ESP32 的透明传输边界。
- 明确 TCP 是连续字节流，不得把单次 `recv()` 当作一帧。

### 3. 接线、默认配置与上电状态

- 只列出与上位机接入相关的 STM32 CDC、STM32↔ESP32 UART 和 ESP32 网络参数。
- 写明串口格式、默认/可配置波特率、TCP/UDP 端口、网络地址来源、冷启动状态和必要前置条件。
- 明确哪些配置持久化、哪些上电恢复默认值；无法静态确认的配置标记待确认。

### 4. 两种上位机接入流程

分别给出：

- USB CDC 直连 STM32 的最小连接、查询、选择实时目标、启动、停止流程。
- 经 ESP32 的 Wi-Fi/TCP 接入流程，包括网络准备、发现或唤醒、TCP 建连、断线重连和命令下行。
- 每一步写出上位机应等待的响应或状态，不描述 UI 操作。
- 说明多 ESP32/多 STM32 场景中连接与设备 UUID 的关系，但不要把现有上位机的数量上限写成固件协议上限，除非固件或协议明确规定。

### 5. SDF1 数据协议

用紧凑表格准确说明：

- V1/V2 帧结构、字节偏移、字段长度、端序、最大 payload/帧长和 CRC 算法。
- message type、flags、UUID、sequence、timestamp、item count 的含义。
- IIS3DWB FIFO word/tag 的解析方法、量程换算和时间重建要求。
- JY61PL payload 的字段顺序、类型和换算公式。
- STATUS 与 CLI_RESPONSE 的解析要点。
- 流解析器必须处理任意分块、粘包、半帧、噪声、CRC 错误、重同步、sequence 回绕和缺口；区分协议错误、链路丢失、实时丢旧和存档数据。

任何数值都必须与当前代码、测试或黄金帧一致。

### 6. AT 命令、状态与重试策略

- 用一个表列出当前支持的命令、参数、允许状态、成功响应、主要错误、是否幂等，以及上位机能否安全重试。
- 明确 CLI 输入是行文本，响应是 SDF1 CLI_RESPONSE 帧，并说明响应所走链路。
- 用简洁 Mermaid 状态图说明 IDLE、ACQUIRE/运行、STOPPING 和配置失败安全态之间的有效转换；状态名称以当前实现为准。
- 解释 LIVESTREAM 目标选择、START、STOP、STATE、BAUD、UUID、watermark、EXPORT、SDCLEAR 的前置条件和副作用。
- 明确查询超时、`ERROR:BUSY`、`ERROR:STATE`、`ERROR:TX_BUSY`、格式/参数错误的处理方式。
- 幂等查询可有限次退避重试；START、STOP、EXPORT、SDCLEAR 等非幂等命令在响应不确定时不得盲目重发。以固件当前实现和测试为最终依据。

### 7. 实时流、SD 存档与导出

- 解释实时数据和板载 SD 权威存档是两条不同语义的路径。
- 说明 UART 与 CDC 实时目标互斥、非目标链路能否继续收发控制响应、STOP 的完成语义，以及 EXPORT 与实时数据的区分方法。
- 说明低带宽 UART 下“丢旧保新”、sequence 缺口和新鲜度的含义，避免上位机把允许的实时丢帧误判为 SD 数据丢失。
- 说明 ARCHIVE_EXPORT 标志、导出顺序、SD 满盘/循环覆盖和 SDCLEAR 等当前行为；只写静态证据能够支持的结论。

### 8. 诊断、错误与恢复

- 汇总 STATUS 和 `AT+STATE?` 中上位机真正需要展示或记录的状态、计数器和诊断字段。
- 给出“现象 → 重点字段 → 可能原因 → 建议动作”的简短表格，至少覆盖：无数据、CRC/帧错误、sequence 缺口、CDC 未连接、TCP 断线、实时拥塞、源侧丢弃、SD 未就绪/需格式化、命令超时和配置失败。
- 区分可重试瞬态、需要用户操作的配置问题和不可安全自动重试的命令。

### 9. 新上位机最小实现清单

列出交付第一版上位机至少必须具备的能力：

- CDC 串口与 TCP 字节流接入。
- 每连接独立的流缓冲、SDF1 解码、CRC 和重同步。
- UUID 识别、sequence/时间戳处理和传感器换算。
- 命令请求与异步数据并行处理、响应关联、超时和幂等控制。
- 状态/错误诊断、实时数据与导出数据分流、原始数据可靠落盘。
- 断线重连、会话/文件分段和日志留痕。

再提供一份精简的静态联调检查清单，并注明真正的端到端吞吐、断线恢复和硬件时序仍需后续实机验收。

### 10. 已知限制与事实来源

- 只列出会影响上位机设计的固件限制、未实现能力和协议风险。
- 给出关键结论的仓库相对路径索引；精确到文件，必要时附符号名或测试名。
- 路径使用反斜杠并放入代码格式，避免依赖仅当前机器可点击的链接。

## 写作要求

- 使用简体中文，专业、直接、可操作；首次出现的缩写给出中文解释。
- 优先使用短段落、步骤和紧凑表格；只保留两张必要的 Mermaid 图。
- 示例必须来自当前协议，不得编造返回值、错误码或报文。
- 不写 UI 设计建议、产品路线图、完整构建教程或固件内部模块详解。
- 不把验收脚本的测试门限误写为通用协议要求；若某门限只适用于现有测试工具，要明确标注。
- 同一事实只在最合适的位置完整说明一次，其他位置使用小节引用。

## 工作步骤

1. 检查三个仓库的 Git 状态、分支、HEAD、目录结构和现有文档。
2. 从 STM32 代码与测试提取线协议、命令、状态机、存储和实时流事实。
3. 从 ESP32 代码与配置提取 UART/TCP 透明传输、发现、连接和重连事实。
4. 用现有上位机解析器与测试进行交叉核对，但不将其作为最高权威。
5. 在内存中建立“结论 → 来源”清单，解决冲突并标出无法静态确认的内容。
6. 直接撰写唯一的最终文档，不生成中间文件。
7. 对最终文档执行质量核验并修正问题。

## 完成前质量核验

至少完成以下检查：

- 每个协议数值、默认参数、命令和错误码均有本地来源。
- STM32 与上位机的 SDF1 字段、CRC、换算公式和黄金帧一致。
- ESP32 的网络配置、端口、UART 参数和连接方向与实际代码一致。
- 命令允许状态、幂等性、超时与重试建议不互相矛盾。
- CDC、UART、Wi-Fi/TCP、实时流、SD 存档和 EXPORT 的边界清楚。
- 文档中没有任何占位标记、空章节、虚构示例、无解释缩写或无来源断言。
- Markdown 表格和 Mermaid 代码块闭合，内部章节引用有效。
- `git diff --check` 通过，且 Git 变更仅包含目标手册文件。

## 验收标准

仅当以下条件全部满足时才可将 Goal 标记完成：

- 已生成 `D:\Codes\SensorHost\docs\上位机固件接口交接手册.md`。
- 接手人员无需通读固件源码，就能据此实现连接、解码、控制、记录、诊断与恢复的最小上位机。
- 单文档覆盖两种接入路径和全部关键接口，但没有无关背景、重复长篇协议或 UI 评价。
- 半成品上位机被正确定位为参考实现，未被当作协议权威。
- 所有不能由静态证据确认的内容均已明确标记，没有猜测性结论。
- 最终回复简要报告生成路径、核验结果、使用的三个仓库提交号，以及仍待实机确认的事项。
